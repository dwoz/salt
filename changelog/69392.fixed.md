Adapt Salt to upstream library API changes pulled in by this dependency
update: pyOpenSSL 26 (CA rebuild via cryptography for v3 extensions and
serial-number truncation; X509Store.add_crl now accepts cryptography CRL
objects directly), pip 26 (InvalidEggFragment fallback for VCS URLs in
pip_state), pytest 8.x compatibility (caplog scoping with logger=, fixture
mark removal, pytest pinned <9 to avoid memory regression on CI runners),
virtualenv 21 (BUNDLE_SHA256 verification), and pyzmq 27 scikit-build-core
(cmake/ninja/protobuf added to onedir --only-binary). Also redacts HTTP
basic auth credentials from get_source_sum error messages.
